pub mod env;

/*
env:
    "[file]:path/to/file"
    "[text]:[text]:Exact text."
    "Or just regular text without a specifier."
    "The [text]: specifier is only needed for text that begins with `[text]:` for whatever reason."
 */
